# macOS
.DS_Store

# Environment & secrets — NEVER commit API keys
.env
*.env

# Python
__pycache__/
*.pyc
*.pyo
.venv/
env/

# R
.Rhistory
.RData

# Logs
*.log

# Large data files (remove lines below if you want to track them in git)
# data/raw/*.csv
# data/raw/*.parquet
# data/raw/*.json

# Generated outputs (regenerated by notebooks — commit manually when ready to share)
outputs/*.tex
outputs/*.png
outputs/*.pdf
# paper/results.html is intentionally NOT ignored so it can be emailed via GitHub

# Jupyter checkpoints
.ipynb_checkpoints/
